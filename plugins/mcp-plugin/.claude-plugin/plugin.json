{
  "name": "shiplight-mcp",
  "description": "Adds a /verify skill for browser testing and local test verification",
  "version": "1.0.0",
  "mcpServers": {
    "browser": {
      "command": "npx",
      "args": ["-y", "@shiplightai/mcp@latest"],
      "env": {
        "PWDEBUG" : "console"
      }
    }
  }
}
